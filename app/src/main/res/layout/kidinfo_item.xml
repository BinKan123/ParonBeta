<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/adviceBar"
        android:layout_width="305dp"
        android:layout_height="52dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="3dp"
        android:layout_marginLeft="0dp"
        android:layout_marginRight="0dp"
        android:layout_marginTop="3dp"
        android:background="@drawable/greenbar"
        android:textAlignment="center"
        app:srcCompat="@drawable/kidinfobtn"
        tools:layout_editor_absoluteX="116dp" />

    <TextView
        android:id="@+id/categoryName"
        android:layout_width="227dp"
        android:layout_height="66dp"
        android:layout_alignBottom="@+id/adviceBar"
        android:layout_alignStart="@+id/adviceBar"
        android:layout_alignTop="@+id/adviceBar"
        android:layout_marginStart="10dp"
        android:gravity="center"
        android:paddingLeft="3dp"
        android:paddingRight="3dp"
        android:text="TextView"
        android:textAlignment="textStart"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="18sp"
        tools:layout_editor_absoluteX="137dp" />

    <Button
        android:id="@+id/removeInfoBtn"
        android:layout_width="25dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/categoryName"
        android:layout_alignEnd="@+id/adviceBar"
        android:background="@drawable/ic_remove"
        android:backgroundTint="@color/text_on_btn"
        android:layout_marginEnd="20dp"
        android:layout_alignTop="@+id/adviceBar" />

</RelativeLayout>